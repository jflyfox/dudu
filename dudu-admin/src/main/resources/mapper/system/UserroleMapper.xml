<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jflyfox.dudu.module.system.dao.UserroleMapper">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id,userid,roleid
    </sql>

    <select id="selectUserrolePage" resultType="com.jflyfox.dudu.module.system.model.SysUserRole">
        <![CDATA[
        select
        t.id,t.userid,t.roleid
        from sys_user_role t
        ${ew.sqlSegment}
        ]]>
    </select>

</mapper>